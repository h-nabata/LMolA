# Security Policy

- Arbitrary shell execution from prompts is forbidden.
- Cloud APIs are not used by default.
- Never commit secrets, model weights, or private datasets.
- Report vulnerabilities via private disclosure to maintainers.
