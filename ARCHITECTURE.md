# Architecture

- CLI layer (`lmola.cli`)
- Schema/config layer (`lmola.schemas`, `lmola.config`)
- Tool adapters (`lmola.tools.*`)
- Validation (`lmola.validation.*`)
- Agent placeholders (`lmola.agent.*`)
